export interface PropertyDetailsInterface extends Document {
  typeVal: string;
  area: Boolean;
  bedrooms: string;
  garages: string;
  nearSubway: Boolean;
  acceptPets: Boolean;
  furnished: Boolean;
  availability: string;
  valueLocation: Number;
}